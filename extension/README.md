# Extension Build

The repository root is the unpacked Chrome extension.

For local development, load this folder in Chrome:

```txt
/Users/john/Documents/Codex/2026-08-28/pl/work/scout-lab
```

The `extension/` folder is reserved for future packaged artifacts if a build step is added.

